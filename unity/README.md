# Unity lives at the repo root

Unity Hub looks for `Assets/` + `ProjectSettings/` on **main**. Those folders are at the repository root so **Add project from repository** works.

Scripts, MCP menu, and the sim contract: [`Assets/BoostPitch.Sim/`](../Assets/BoostPitch.Sim/)

PennyLane kickoff sidecar (simulation_only): [`tools/pennylane_kickoff.py`](tools/pennylane_kickoff.py)
